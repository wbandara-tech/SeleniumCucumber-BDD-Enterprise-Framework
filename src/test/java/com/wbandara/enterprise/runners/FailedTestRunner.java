package com.wbandara.enterprise.runners;

import io.cucumber.junit.Cucumber;
import org.junit.runner.RunWith;

@RunWith(Cucumber.class)
public class FailedTestRunner {
    // JUnit4 runner for failed scenarios (rerun)
}
